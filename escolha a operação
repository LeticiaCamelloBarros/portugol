
// Um determinado sistema solicita que o usuário insira dois valores inteiros.
// Logo após, o sistema apresenta três opções de operações que poderão ser
// executadas, o usuário precisa escolher uma delas:
// [S] Soma
// [M] Multiplicação
// [B] Subtração.
// Caso contrário, se for uma opção inválida, o Sistema precisa notificar.

// Caso contrário, se for uma opção inválida, o Sistema precisa notificar.

// Considere o caractere maiúsculo ou minúsculo.


programa {
  funcao inicio() {
    cadeia choice 
    real n1 , n2 , acao

   escreva("escolha uma operação para ser realizada : \n")
   escreva("[S]-soma \n[M]-multiplicação \n[D]-diferença \n")
   leia(choice)

   escreva("digite os dois números : \n")
   leia(n1)
   leia(n2)
    se(choice == "S" ou choice == "s"){
       acao = n1 + n2 
  }senao se (choice == "M" ou choice == "m"){
       acao = n1 * n2 
  }senao se (choice == "D" ou choice == "d"){
       acao = n1 - n2 
  }

  escreva("resultado : " , acao)
